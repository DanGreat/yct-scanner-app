import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yabatech.appscanner',
  appName: 'yct-scanner',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
